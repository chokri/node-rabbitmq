version: "3.8"

services:

  api:
    build: .
    ports:
      - 3000:3000
    volumes:
      - ./:/app

  rabbitmq:
    image: rabbitmq:3-management-alpine
    ports:
        - 5672:5672
        - 15672:15672
